Commit all staged changes and push to the current branch.

Requirements:
- Use conventional commit format (feat, fix, chore, docs, refactor, test)
- Write a concise commit message based on the diff
- Push to the current branch's upstream

If $ARGUMENTS is provided, use it as the commit message instead of generating one.
