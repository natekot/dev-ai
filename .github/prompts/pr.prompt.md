---
agent: 'agent'
description: 'Create a pull request with a generated description'
---
Compare the current branch against the default branch. Read the commit log and diff. Generate a PR title (conventional-commit style) and a structured description with a summary, list of changes, and test plan. Create the PR using `gh pr create`.
